# ============================================================================
# CONVOY backend
#
# INTERPRETER: Python 3.12.x  (3.11 also fine)
#
# Do NOT use Python 3.13+ or 3.14. Several dependencies ship compiled
# extensions (pydantic-core is Rust/PyO3; asyncpg and cryptography are C) and
# publish prebuilt wheels only for interpreter versions they support. On a
# newer interpreter pip falls back to compiling from source, needs a full Rust
# toolchain, and then fails anyway. Check first:   python3 --version
# ============================================================================

# ---- Phase 2A: pure logic core ---------------------------------------------
cryptography==42.0.8
pydantic==2.8.2
pydantic-settings==2.4.0

# ---- Phase 2B: persistence -------------------------------------------------
SQLAlchemy[asyncio]==2.0.32
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.2
greenlet==3.0.3

# ---- dev -------------------------------------------------------------------
pytest==8.3.2
pytest-asyncio==0.24.0
pytest-cov==5.0.0

# ---- Phase 2B-2: broker ingestion ------------------------------------------
aiomqtt==2.3.0
structlog==24.4.0

# ---- Phase 3: API + realtime -----------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.6
websockets==13.0.1